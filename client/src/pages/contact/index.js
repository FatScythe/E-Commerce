// Hooks
import useTitle from "../../hooks/useTitle";

const Contact = () => {
  useTitle("Contact Us");

  return (
    <section id='contact' className='container'>
      <h3>Contact</h3>
    </section>
  );
};
export default Contact;
